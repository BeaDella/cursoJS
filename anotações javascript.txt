DOCUMENTAÇÃO: https://developer.mozilla.org/pt-BR/docs/Learn/JavaScript/First_steps/What_is_JavaScript


alert(); - exibe uma mensagem na tela

let nomeVariavel = ; - define uma variável

let nomeVariavel = prompt(''); - exibe uma tela para o usuário fazer um imput e armazena o valor em uma variável

console.log(''); - exibe uma mensagem no console de inspecionar elemento

// - comentário

Template Strings (`mensagem qualquer ${variavel}`); - identação que permite o chamado de uma variável no texto

operadores
AND (A>B) && (B==C): se ambas forem verdadeiras, o resultado será verdadeiro
OR (A>B) || (B==C): se ao menos uma das condições for verdadeira,o resultado será verdadeiro

operador ternário: condição ? se sim : se não

Math.random() - função que gera um numero pseudoaleatório entre 0 e 1

parseInt() - pega apenas a parte inteira do número passado como parâmetro

variável = document.querySelector('h1') - seleciona uma seção do documento html e atribui à variável

variável.value = pega o valor da seção do documento atribuída à variável

innerHTML - te permite mexer no html correspondente àquela variável

function nomeDaFuncao() {}

getElementById('')

removeAttribute('nome do atributo')

setAttribute('nome do atributo', valor dele)

var array = ['teclado', 'mouse'] - cria uma lista

array.length - retorna quantos elementos a lista possui

first = array[0] - atribui o primeiro elemento (teclado) à variável first

array.includes(first) - verifica se a lista possui o elemento referenciado

array.push('mousepad') - insere o elemento referenciado ao final da lista

array.pop(); - remove o último elemento da lista